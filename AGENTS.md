# Project scope

Deploy application code and database structure only.
Do not import, seed, upload, publish, or embed any old examination content until the user explicitly instructs it.
User-supplied MEQ source files refer to academic year 2568, examination year 2569. Keep these separate.
Local work/ and app/design-preview/ contain non-production reference material and must never be committed or deployed.
Keep MCQ and structured MEQ case banks separate. Timed MEQ delivery uses the separate exam_delivery engine with server-controlled stages.
